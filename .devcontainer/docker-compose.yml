services:
  devcontainer:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TZ: ${TZ:-America/New_York}
        ZSH_IN_DOCKER_VERSION: "1.2.0"
    init: true
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
