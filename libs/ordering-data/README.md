# Ordering Data

EF Core DbContext, entity definitions, and migrations for the ordering domain.

## Scope

Orders, order items, order status, customer details, and order history.

## Used By

- `ordering-api` — reads and writes orders
- `order-processing-functions` — order state transitions
